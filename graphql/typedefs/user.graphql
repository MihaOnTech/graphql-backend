# Types
type User implements Timestamps {
    id: ID!
    name: String!
    password: String!

    # Interface required
    createdAt: Date!
    updatedAt: Date!
}

# Inputs
input UserCreateInput {
    name: String!
    password: String!
}

input UserUpdateInput {
    name: String
    password: String
}